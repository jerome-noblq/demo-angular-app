import { Component } from "@angular/core";

@Component({ //passed configuration object between them
    selector: 'app-header',
    // standalone: true,
    templateUrl: './header.component.html',
    styleUrl: './header.component.css'
})

export class HeaderComponent{};